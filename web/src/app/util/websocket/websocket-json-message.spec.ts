import { WebsocketJsonMessage } from './websocket-json-message';

describe('WebsocketJsonMessage', () => {
  it('should create an instance', () => {
    expect(new WebsocketJsonMessage()).toBeTruthy();
  });
});
